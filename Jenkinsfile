pipeline {
    agent any
    triggers {
        pollSCM '*/5 * * * *'
    }
    stages {
        stage('Build') {
            steps {
                // One or more steps need to be included within the steps block.
            }
        }
        stage('Run') {
            steps {
                // One or more steps need to be included within the steps block.
            }
        }
        stage('Test') {
            steps {
                // One or more steps need to be included within the steps block.
            }
        }
        stage('Finalize') {
            steps {
                // One or more steps need to be included within the steps block.
            }
        }
    }
}